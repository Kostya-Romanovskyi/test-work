import { StyledButton, ButtonWrapp } from "./BackButton.styled"

const BackButton = () => {
    return (
        <ButtonWrapp>
            <StyledButton to='/'>Back to home</StyledButton>
        </ButtonWrapp>
    )
}
export default BackButton